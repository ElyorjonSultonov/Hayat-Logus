export interface IGoods {
  code: string;
  name: string;
  manafacture: string;
  measurements: string;
  packs: number;
  barcode: number;
  receipt: boolean;
  sprav: boolean;
  mx: string;
  fom: string;
  fom_group: string;
  fix: number;
}
